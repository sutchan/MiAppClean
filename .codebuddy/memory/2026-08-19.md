# 2026-08-19 工作记录

## 项目审查：MiAppClean（v1.15.0）

完成对 MiAppClean 仓库的静态代码审查，发现以下关键问题（详见审查报告）：

### 严重 / 功能性缺陷
1. **根 `index.html` 缺少设置抽屉与卸载警告 DOM**：根 `index.html`（线上站点）有 `#settingsBtn`、`#langBtn`，但**没有** `#settingsOverlay`/`#settingsDrawer`/`#settingsPanel`/`#settingsClose`/`#uninstallWarn`/`#toast` 等结构。而 `app/index.html`（应用原型）第147-170行包含完整结构。`app.ui.js init()` → `MiSettings.bindDrawer()` 与 `app.ui.handlers.js confirmUninstallWarn` 依赖这些元素，在根站会静默失效（设置打不开、卸载确认无弹层）。需把 app 原型中的抽屉/弹层/toast DOM 同步到根 `index.html`。

2. **主题系统双实现冲突**：根 `index.html` 第52行加载 `/prototype/theme.js`（THEME_COLOR 橙色 #ff6900，独立 localStorage 键逻辑），第176行前又加载 `app/settings.js`（同一 `miac-theme` 键，THEME_COLOR 白/黑）。两者都写 `data-theme`，后者覆盖前者；且 `app/index.html` 第34行有 `#themeBtn` 但根 `index.html` 没有，导致根站主题切换按钮事件在 `app.ui.js` 找不到元素。`theme.js` 的 `injectThemeFab()` 因检测到 app 原型的 `#themeBtn` 而跳过注入。两套主题逻辑并存易冲突，建议统一为 `settings.js`（app 主线）。

### 版本号不一致（违反 SemVer 规则）
- 头注释停留在旧版本的文件：`app/app.state.js`(v1.14.0)、`app/generate.js`(v1.14.0)、`app/app.ui.handlers.js`(v1.14.0)、`app/app.ui.labels.js`(v1.14.0)、`app/app.share.js`(v1.14.0)、`app/render.js`(v1.14.2)、`app/i18n.js`(v1.14.2)、`app/i18n.dict.js`(v1.15.0 实际已更新)、`app/css` 聚合头 v1.13.4。
- `VERSION` 文件当前为 `v1.15.0`，索引 HTML 与多数 app 模块已到 v1.15.0，但上述子模块头注释未同步。按用户规则「每次修改 bump 且仅更新被改文件头注释」，这些文件若未被本次修改则无需改；但 CHANGELOG 需有 v1.15.0 小节且 README/openspec 对齐。

### 代码异味
- `app/app.state.js:111` `setRiskFilter` 中 `_search = _search; // no-op` 死代码，应删除（保持接口可改为空函数体）。
- 多处 `document.querySelector("#catList")` 在文件顶层（模块加载时）执行，若脚本在 DOM 就绪前加载会返回 null。`app.state.js` 顶层 `catList`/`stat` 在脚本末尾 `return` 保护，但 `app.ui.js` 等也需确认加载顺序（index.html 脚本置于 body 末尾，安全）。
- `generate.js` 解析 `#custom` textarea 与 `getChecked` 的并集合并逻辑需与 `app.state.js` 一致（未在本次深入读 `generate.js` 全文，已读前段：遍历 device→groups→items，仅 selectable 入命令）。

### 文档与结构
- `openspec/project.md` 正确（引用 `app/app.share.js`、`docs/governance/` 均真实存在）。`openspec/spec.md` 不存在（实际用 `specs/` 子目录 + `project.md` + `AGENTS.md`），README 未误引 spec.md。
- README 顶部版本徽章与 CHANGELOG 须与 v1.15.0 对齐（本次未逐行核对 README 全量，已确认 README 引用 `.github/`、`theme.js`、`app/` 路径大体存在）。
- `prototype/` 实际仅 `index.html/README.md/theme.js/components/design-system/interaction/archive`，与 README/openspec 描述基本一致。

### 建议优先修复项
1. 将 `app/index.html` 的设置抽屉/卸载弹层/toast DOM（第147-170行）补全到根 `index.html`。
2. 统一主题逻辑（移除根 index.html 对 `prototype/theme.js` 的依赖，或让 `settings.js` 接管 `themeBtn` 事件）；给根 index.html 补 `#themeBtn` 或调整 `theme.js` 注入策略。
3. 清理 `app.state.js` 的 `_search = _search` no-op。
4. 核对并补齐 v1.15.0 相关 CHANGELOG/README 版本展示位。

## 追加审查：当前工作树静态复核
- 复核确认高优先级缺陷仍存在：Python/CI 用 Python `eval` 解析含未加引号 JS 键名的 `apk-data.js` 会失败；执行函数还会拼出 `adb adb shell ...`。
- 前端仍存在卸载确认取消不回退、复制二次确认条件不可达、自定义包名未校验且未知包默认 safe、平板全选取不到 phone 数据等风险。
- 设置面板存在遮罩点击冒泡导致点击面板即关闭、默认模式/记忆勾选/恢复默认设置未真正作用于应用状态、主题/重置事件派发到 document 而监听在 window 等状态同步问题。
- 文案刷新用 `textContent` 覆盖带 `<strong>/<code>` 的安全提示；CI 仅做有限版本校验，README/CONTRIBUTING/OpenSpec 仍有旧路径和旧数据结构描述。

## 修复轮次：MiAppClean 升级至 v1.15.1

对 v1.15.0 审查结果执行修复 + 规范对齐（属 patch 级，bump 至 v1.15.1）：

### 真实 BUG 修复
1. **`app/index.html` `#toast` 初始 `hidden` 导致所有轻提示不显示**：`toast()` 仅用 `.show` 类控制显隐，不移除 `hidden`。已移除初始 `hidden`（与根 `index.html` 行为对齐）。
2. **`app/index.html` 步骤2 缺 `#uninstallBanner` 常驻警告条**：`toggleUninstallWarn()` 因找不到元素静默 `return`，卸载模式无常驻风险提示。已在步骤2 panel 内补齐（与根站及 spec「卸载模式常驻红色警告」对齐）；确认 `#uninstallBanner`(常驻条) 与 `#uninstallWarn`(确认弹层) ID 已区分无冲突。
3. **`app/i18n.dict.js` `notice.title` 中/英值均为空字符串**：导致 `app/index.html` 与根 `index.html` 的「风险提示」标题渲染为空。填入 `zh-CN:风险提示 / en-US:Risk Notice`。已用脚本校验全 70 条字典双语文案非空 → 通过。

### 规范对齐 / 文档
- README 目录结构移除已删除的 `theme.js` 过时条目（v1.15.0 已移除外链，改内联防闪烁脚本，主题单一源为 `app/settings.js`）。
- 补充语义化 id：`app/index.html` 新增 `mainWrap`/`resultToolbar`/`riskNotice`/`searchRow`。
- 无超过 200 行需拆分的源文件（仅 `apk-data.js` 164 条目数据契约，豁免拆分）。

### 版本统一（v1.15.0 → v1.15.1）
- 全局单一来源：`VERSION`、根 `index.html` 头注+页脚、README、docs/commands.md、prototype/README.md、apk-data.js 头注、scripts(.bat 标题/.py 头注+打印) 全部同步 v1.15.1。
- 落后头注修正：app/i18n.js(v1.14.2→.1)、app/app.share.js(v1.14.0→.1)、app/app.ui.labels.js(v1.14.0→.1)、app/render.js(v1.14.2→.1)、app/generate.js(v1.14.0→.1)、app/app.css(v1.13.4→.1)；其余 app/*.js 头注与 prototype 文档头注同步至 v1.15.1。
- CHANGELOG 新增 `[1.15.1]` 小节（fix + docs）。

### 验证结果
- 版本一致性 grep 9 个 CI 校验位全部命中 v1.15.1。
- `apk-data.js` 解析：164 包名、0 缺失字段、风险分级 danger/caution/safe 齐全（CI Python eval 逻辑可解析）。
- 关键 JS 文件 `node --check` 语法通过（generate/state/render/i18n 等）。
- i18n 字典 70 键双语文案非空校验通过。
- 注：prototype/* 文档页 `v1.0.0`/`v1.6.0`(archive) 为各自文档自版本，非产品版本，按规范不动。

## code-review 轴审查 + 修复轮次：MiAppClean 升级至 v1.15.2

用 code-review 技能对 v1.15.1 提交（fixed point `aeaf497`=v1.15.0）跑 Standards+Spec 双轴子代理审查，核实后确认 3 处真实 bug 并修复，bump 至 v1.15.2：

### 真实 BUG（均经实际文件核实，非子代理失真）
1. **toast 常驻空框**：v1.15.1 移除 `#toast` 初始 `hidden` 后，`app.components.css` 的 `.toast` 既无默认隐藏态也无 `.toast.show` 规则 → 由「永不显示」变「常驻显示空灰框」（根 index 与 app 共用该样式）。修复：`.toast` 补 `opacity:0; pointer-events:none` + `.toast.show{opacity:1}`，与 `toast()` 的 `.show` class 控制对齐。
2. **卸载常驻警告条永不显示**：`#uninstallBanner` 初始用 class `.hidden`（CSS `.warn-banner.hidden{display:none}`），但 `toggleUninstallWarn()` 操作 `hidden` **属性**、未移除 class → 切卸载时 class 仍在、条不显示。修复：统一 `banner.classList.toggle("hidden", !show)`，与 CSS 契约一致（app 与根 index 均受益）。
3. **JSON-LD `softwareVersion` 版本遗漏**：根 `index.html` 的 JSON-LD 仍 `1.15.0`，违反 site/spec 版本同步要求。已同步。

### 子代理失真校正（重要）
- Spec 子代理误报「`notice.title` 空文案未修」——实际 v1.15.1 已修（i18n.dict.js 已是 风险提示/Risk Notice），其读取了旧缓存。**必须以实际工具输出为准**（呼应记忆 52823145）。

### 版本统一（v1.15.1 → v1.15.2）
- `app/app.components.css` 头注从 v1.9.0 一次性校正至 v1.15.2（此前严重落后）。
- 其余 app/*.js、app.css、app.base.css 头注及全部全局展示位（VERSION/README/docs/commands/prototype/scripts/apk-data/CONTRIBUTING/governance/prototype 文档页）统一 v1.15.2。
- CHANGELOG 新增 `[1.15.2]` 小节（fix ×3 + docs）。

### 验证
- 版本一致性 9 个 CI 校验位全部命中 v1.15.2。
- 被改 JS `node --check` 通过；lint 仅存 2 条 HINT（`execCommand` 弃用、`settings.js` 未用变量 drawer），均预存、非本次引入，未改动。
- `toast` CSS 校验脚本因分号差异误报 false，实查 `.toast{opacity:0}` + `.toast.show{opacity:1}` 均正确存在。
