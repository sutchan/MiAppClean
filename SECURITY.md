# 安全政策（Security Policy）

## 支持版本

本仓库为社区维护的开源工具集，以下版本接收安全相关更新：

| 版本 | 状态 |
|------|------|
| `v1.6.x`（最新） | ✅ 维护中 |
| `< v1.6.0` | ❌ 已停止支持 |

## 漏洞举报

如发现安全漏洞，**请勿在公开 Issue 中披露**，以免被恶意利用。
请通过以下私密渠道报告：

- **GitHub Security Advisory**：在仓库 `Security → Report a vulnerability`
  提交私有漏洞报告（推荐，维护者会收到私密通知）。
- 若不便使用 Advisory，可在仓库中提 Issue 并 @ 维护者，明确标注
  `security-report`，维护者将转为私密处理。

请在报告中尽量包含：
1. 受影响版本与运行环境（Windows / Python 版本 / adb 版本）；
2. 漏洞描述与复现步骤；
3. 潜在影响范围；
4. 如有，附上修复建议。

## 处理流程

1. 确认收到报告（通常在 72 小时内响应）。
2. 评估严重性与影响范围，必要时与报告者沟通细节。
3. 在私有分支修复并编写回归验证。
4. 发布修复版本，并在 `CHANGELOG.md` 中标注；
   对公开披露将配合报告者协商 CVE 与披露时间。

## 安全使用提示

- 本工具通过 `adb shell pm` 操作设备，**存在误删系统应用导致设备异常的风险**。
- 精简前务必使用 `xiaomi-apk-cleanup.py backup` 或备份工具留存完整快照。
- 仅从本仓库官方地址获取脚本，勿执行来源不明的 `adb` 批量命令。

---

> 版本：`v1.6.12`
